# Elasticsearch — defaults to local if not set
ELASTICSEARCH_URL=http://localhost:9200

# Ollama — optional, used for JSON schema analysis
# If unavailable the app falls back to generic field classification
OLLAMA_URL=http://localhost:11434/api/generate
OLLAMA_MODEL=llama3.2:3b
